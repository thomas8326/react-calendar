import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class Hint extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

Hint.propTypes = propTypes;
export default Hint;
